#include<stdio.h>

void main(){

    int radius,hight,Acir,Acyl;
    printf("Enter the radius of Circle : ");
    scanf("%d",&radius);
    Acir = (radius*radius)*3.14;
    printf("The Area of the Circle is %d",Acir); 
}