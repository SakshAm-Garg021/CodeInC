#include<stdio.h>

void main(){

    int a=4;
    int b=3;
    int c=a+b;
    printf("The value of a is %d, b is %d and it sum is %d\n",a,b,c); 
    // Modulas(%) is use to find the reminder 
    printf("the reminder of a divided by b is %d\n",a%b);
}